@using MajorPrjt.Web.Areas.User.ViewModels
@model UserForumViewModel

<link href='https://fonts.googleapis.com/css?family=Yesteryear' rel='stylesheet'>
@{
}

<h1>Search Your Questions</h1>

<div  class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="CategoryId" class="control-label"></label>
                <select asp-for="CategoryId" class ="form-control" asp-items="ViewBag.CategoryId"></select>
            </div>

            <button type="submit" class="btn btn-primary">Fetch Questions</button>
        </form>
    </div>
</div>


@if(Model != null && Model.Topics != null)
{
    <form method="post">
        <div class="card-columns mt-3">
            @foreach(var item in Model.Topics) 
            {
                <div class="card">
                    <div class="card-body">
                        <div class="card-title font-weight-bolder"> @item.Title </div>
                        <div class="card-text text-danger font-weight-bolder">  @item.PostDateTime</div>
                        <div class="card-text font-italic"> @item.Description </div>
                        <div class="card-text font-italic"> @item.Comments </div>
                    </div>
                    <div class="card-footer">
                        <button class="btn btn-sm btn-primary " style="width:100px; height:32px" ><a class="text-light" asp-area="DForum" asp-controller="Comments" asp-action="Create">Comment</a>
                            </button>
                        <button class="btn btn-sm btn-primary " style="width:100px; height:32px" ><a class="text-light" asp-area="DForum" asp-controller="Replies" asp-action="Create">Reply</a>
                        </button>
                        <button class="btn btn-sm btn-danger" type="submit">Cancel</button>
                    </div>
                </div>
            }
        </div>
    </form>
}


@section MyView {
    <style>
        h1 {
            color: #F1F114;
            text-align: center;
            font-family: 'Yesteryear';
            font-size: 2.5rem;
        }
        #tagg{
            text-align: center;
        }
    </style>
}



